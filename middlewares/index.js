//* Unificar RUTAS para importar
/*
const fieldsValidation = require("../middlewares/fields-validation");
const validateJWT = require("../middlewares/jwt-validation");
const rolValidation = require("../middlewares/rol-validation");

//* Exportar rutas
module.exports = {
  ...fieldsValidation,
  ...validateJWT,
  ...rolValidation,
};
*/